# Memory

- 2026-07-03: `SwiftWABackupAPI` is the maintained implementation. `/Users/domingo/Programacion/PyWABackupAPI` is frozen as legacy code and should not be updated in parallel unless the user explicitly requests a one-off Python change.
